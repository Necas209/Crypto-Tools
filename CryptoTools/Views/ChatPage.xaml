<Page x:Class="CryptoTools.Views.ChatPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:vm="clr-namespace:CryptoTools.ViewModels"
      xmlns:local="clr-namespace:CryptoTools.Views"
      mc:Ignorable="d"
      Title="ChatPage" Height="450" Width="800">
    <Page.DataContext>
        <vm:ChatPageViewModel></vm:ChatPageViewModel>
    </Page.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Label Content="Enter your name:" Margin="5" />
        <TextBox x:Name="NameTextBox" Margin="5" Grid.Row="0" Grid.Column="1" />

        <Label Content="Enter your message:" Margin="5" Grid.Row="1" />
        <TextBox x:Name="MessageTextBox" Margin="5" Grid.Row="1" Grid.Column="1" />
        <Button Content="Send" Margin="5" Grid.Row="1" Grid.Column="2" Click="SendButton_Click" />

        <Label Content="Chat messages:" Margin="5" Grid.Row="2" />
        <ListBox x:Name="ChatListBox" Margin="5" Grid.Row="3" Grid.ColumnSpan="3" ItemsSource="{Binding ChatMessages}"/>
    </Grid>
</Page>
