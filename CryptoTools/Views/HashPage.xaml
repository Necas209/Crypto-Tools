<Page x:Class="CryptoTools.Views.HashPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:vm="clr-namespace:CryptoTools.ViewModels"
      mc:Ignorable="d"
      Title="HashPage" Height="450" Width="800">
    <Page.DataContext>
        <vm:HashPageViewModel />
    </Page.DataContext>
    <Grid>
        <ComboBox Width="200" VerticalAlignment="Top" Margin="0,20,0,0"
                  ItemsSource="{Binding HashingAlgorithms}"
                  DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding HashingAlgorithmId}" />
    </Grid>
</Page>