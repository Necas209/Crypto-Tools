<Window x:Class="CryptoTools.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CryptoTools.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" MinHeight="540" MinWidth="830" SizeToContent="WidthAndHeight">
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>
    <Grid>
        <Grid>
            <TextBlock HorizontalAlignment="Center" Margin="0,0,0,0" FontSize="16">
                <Bold>CryptoTools</Bold>
            </TextBlock>
            <Button x:Name="LogoutButton" HorizontalAlignment="Right" VerticalAlignment="Top" Content="Logout"
                    Width="100" Margin="0 10 25 0" Height="30"
                    Click="LogoutButton_Click" Visibility="Hidden" />
        </Grid>
        <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden"></Frame>
    </Grid>
</Window>